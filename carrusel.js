$(window).on("load",carrusel);

function carrusel(){
	
	$('.carrusel').slick({
   
  autoplay: true,
  autoplaySpeed: 3000,
  fade: true,
  cssEase: 'linear',
  arrows:false
  });
	
	
	
}